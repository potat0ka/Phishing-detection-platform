# AI Phishing Detection Platform - Environment Configuration
# Copy this file to .env and fill in your actual values

# Flask Application Settings
FLASK_SECRET_KEY=your_random_secret_key_here_change_this_in_production

# Database Configuration Options (Choose ONE):

# Option 1: MongoDB Atlas (Cloud Database - Recommended for Production)
# Get connection string from MongoDB Atlas dashboard: "Connect" -> "Connect your application"
# MONGODB_URI=mongodb+srv://username:password@cluster.mongodb.net/phishing_detector?retryWrites=true&w=majority

# Option 2: Local MongoDB (Recommended for Development)
# Make sure MongoDB is installed and running on your system first
# See README.md for MongoDB installation instructions
MONGODB_URI=mongodb://localhost:27017/phishing_detector

# Option 3: Local Storage (Default - No MongoDB Required)
# Leave MONGODB_URI commented out to use local JSON file storage
# This option works out-of-the-box without any database setup

# User Data Encryption (Auto-generated if not set)
# Generate a secure 32-character key for production use
USER_ENCRYPTION_SECRET=your_encryption_key_here_change_this_in_production

# Default User Accounts (Change passwords after first login)
# Super Admin: username=admin, password=admin123
# Sub Admin: username=subadmin, password=subadmin123  
# Regular User: username=user, password=user123

# Email Configuration (Optional - for password reset functionality)
# SMTP_SERVER=smtp.gmail.com
# SMTP_PORT=587
# SMTP_USERNAME=your_email@gmail.com
# SMTP_PASSWORD=your_app_password

# External API Keys (Optional - for enhanced threat detection)
# VIRUSTOTAL_API_KEY=your_virustotal_api_key_here
# URLVOID_API_KEY=your_urlvoid_api_key_here

# Application Settings
DEBUG=True
PORT=8080
HOST=0.0.0.0